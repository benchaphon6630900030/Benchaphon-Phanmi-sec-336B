#1-4
num = int(input("Enter the value is: "))

def harmonic_sum(num):
	total_sum = 0.0
	for i in range(1, num+1):
		total_sum = total_sum + 1/i;
	return total_sum;
        
print("Limit = ",(num))
print("value = ",round(harmonic_sum(num), 2))
